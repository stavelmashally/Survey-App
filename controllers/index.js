const authCtrl = require('./authController');
const billingCtrl = require('./billingController');
const surveyCtrl = require('./surveyController');

module.exports = {
  authCtrl,
  billingCtrl,
  surveyCtrl,
};
